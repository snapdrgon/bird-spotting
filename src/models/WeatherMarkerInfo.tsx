export interface IWeatherMarkerInfo {
    WeatherMarkerInfo: [
        {
            Condition: string;
            Temperature: string;
            Degrees: string;
            Wind: string;
            At: string;
            Gusts: string;
            Speed: string;
            Max: string;
            Min: string;
        }
    ]
}
